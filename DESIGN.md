# 一纸 · 余生：设计与维护

## 概念与阅读路径

把照片里的两个人，写进一封可以亲手打开、逐页阅读的长信。第一眼是铺满屏幕的照片，翻开封面后留出停顿；照片与心意各有阅读空间，最后让亲友把赴约信息带走。

视觉以象牙纸、朱红墨、庭院与花园照片为基础。四封短笺分别采用信笺、便笺、明信片和书页；心意的扇形展开延续纸张意象，直立数字让赴约信息清楚可读。纸粒保持静止，文字使用项目文案。

| 阅读阶段  | 呈现与操作                                                               |
| --------- | ------------------------------------------------------------------------ |
| 01 / 一纸 | 朱敏在左、汪家喆在右；完整照片封面沿左侧书脊向左打开                     |
| 展信内页  | 固定信页呈现“从此，每一页都有你”；继续按钮获得焦点，宾客决定何时下滚     |
| 02 / 相伴 | 四张照片原生纵向排列，点击打开对应信纸；可以连续读信，关闭后回到触发处   |
| 03 / 心意 | 手机在双喜邀请下方扇形展开两张纸页；桌面从中央向两侧展开，可放大阅读全文 |
| 04 / 赴约 | 日期、两段时间、完整店名与独立厅号，提供地图选择、图片预览和保存         |

浏览器负责原生滚动。固定 SVG 箭头依次引导照片、心意与赴约；最后章节每次继续下移约 72% 视口，实际到底才变为返回。展开地图或上滚会重算到底状态，封面和弹窗期间隐藏固定箭头。站名、首章、返回入口和离开后手动滚回顶部均可重置封面，方便重读。

## 沿左侧书脊打开

封面是一整张照片纸页，内页是位于其下方、始终不动的信页。`OpeningCover.vue` 将 `.cover-page` 与 `.cover-sheet` 并列放置；`CoverFront.vue` 在封面上只渲染一个响应式照片元素。`src/styles/cover.css` 把透视原点与旋转原点都放在 `left center`，令封面从 0° 向左打开至 −180°。转过纸张边缘后，封面移出视口左侧，原位信页完整显露；开场完成时隐藏封面。

内页上的投影随封面边缘移动：`--cover-edge` 控制阴影位置，`--cover-shadow` 控制强度。整个过程中没有移动或缩放信页。页首导航只在 `opening` 阶段隐藏，防止文字横跨运动中的纸张。

照片页正反面使用 `backface-visibility`，同时按 `data-side` 显式隐藏背向观众的一面，处理 WebKit 的镜像背面绘制。封面背面为空白纸面；信文属于独立的 `.cover-page`。关闭时仅封面可交互，完全打开后才启用内页；`inert` 与 `aria-hidden` 同步交互和无障碍状态。重置清零翻转、边缘与阴影进度，并恢复正面。

宽度不超过 700px 时开场约 1.9 秒，其他视口约 2.4 秒。完成后停留原处，“继续读我们的故事”获得焦点且不触发滚动。“直接看照片”跳过开场；滚轮、触控、滚动键或 Escape 可结束动画。减少动态效果直接显示内页，同样由宾客决定何时继续。

Vue 应用挂载后才启用展信与保存入口。`App.vue` 打开信件前先停止当前滚动，避免手机快速操作时弹窗锁定了错误的阅读位置。

## 信纸与心意

照片顺序、短笺原文、`paper` 类型与 `carrier` 名称由 `src/data/wedding.ts` 的 `photographs` 保存。

| 照片                      | 原文                                       | 纸面与字形                                                                               |
| ------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------- |
| 在你身旁 · courtyard      | 人间的风景很多，<br>我偏爱有你的这一处。   | `letter` / 一封信笺：暖白折痕、横线、称呼与落款，LXGW WenKai GB                          |
| 把爱写成日常 · promise    | 那些平凡的小事，<br>因为是你，都值得珍藏。 | `memo` / 一页日常：淡黄方格、装订侧边、微斜毛笔字，Letter Brush                          |
| 往后的每一页 · together   | 不用赶路。<br>往后的日子，我们慢慢走。     | `postcard` / 一张明信片：浅绿纸面、日期邮戳、分隔线与落款，LXGW WenKai GB                |
| 都想和你一起 · a-long-way | 这一页写我们，<br>下一页，想请你也在场。   | `book` / 一页余生：米色书页、书脊、竖排标题与并列正文，Letter Serif，签名 LXGW WenKai GB |

“03 / 心意”的两封原信文为“把平凡的日子，过成我们的日子。也把这份欢喜，郑重地交给你。”和“有些欢喜，想当面说。有些时刻，想与你一起。”，独立于四张照片的短笺。

`CorrespondenceSections.vue` 把心意纸页放在稳定的 `.heart-letters` 容器内；`src/composables/usePaperFan.ts` 观察该容器，避免运动中的纸页反复触发自身动画。可见比例达到 18% 时从 `folded` 进入 `opening`，完全离开视口后等待下次重播。桌面从中央带 Y 轴透视向两侧展开，每页 1,500 毫秒；手机从双喜邀请下方的重叠卡牌展开，每页 1,250 毫秒。右页延迟 90 毫秒，动画结束后进入 `open` 并释放 `will-change`。

纸页落定后留出阅读间隙，手机角落箭头避开署名。键盘焦点进入时立即展开，点击先稳定纸页再打开对应信件；持有焦点时保持展开。减少动态效果直接显示最终状态，动画不会成为阅读的前提。

## 字体、箭头与邀请图片

中文手写字、签名及“囍”使用 **LXGW WenKai GB Regular v1.522**，完整保留“汪家喆”中的“喆”。GB 源字体、授权和字形记录保存在 [assets/font-sources/lxgw-wenkai-gb/](assets/font-sources/lxgw-wenkai-gb/README.md)。书页正文使用 Letter Serif，便笺正文使用 Letter Brush。

正式邀请和 PNG 的 `10.03`、`17:28`、`17:58` 使用 **Noto Serif SC Regular 直立数字子集**，CSS 字族为 `Wedding Numerals`。同组数字共用字号、基线，采用比例宽度；本地文件 `src/assets/wedding-numerals.woff2` 为 2,216 字节、12 字符，倾角为零。来源、哈希与度量见 [wedding-numerals.json](assets/font-sources/wedding-numerals.json)，授权复用 `public/fonts/OFL.txt`。

正式邀请与 PNG 的非强调文字为纯黑 `#000000`，日期的“03”、时间的分钟及强调文字为朱红 `#902e2c`。网页通过 `.invitation` 的局部颜色变量实现，其他照片与信纸保留各自纸面风格。日期与时间的强调部分保持直立字形。

全部可见箭头由 `ArrowIcon.vue` 的 SVG 路径绘制，`src/styles/arrows.css` 管尺寸与地图展开标记。箭头以 `currentColor` 跟随控件文字，使用 `aria-hidden` 且不可聚焦；按钮、链接或无障碍名称提供操作含义。地图折叠项隐藏原生标记，展开时旋转共用 SVG，因此系统字体变化不会改变箭头字形。

`src/lib/invitation-image.ts` 等待 GB、Letter Serif 与 Wedding Numerals 字体就绪后，按 `src/data/wedding.ts` 绘制 **1080×1620 PNG File**。日期数字为 190px，时间数字为 86px，黑红部分以实际测量宽度在同一基线上居中。内容包括姓名、双喜、公历与农历、17:28 到场、17:58 仪式、完整店名“黄山市歙县徽苑一楼”与“2号厅”。独立画布使保存结果不受手机尺寸或当前滚动位置影响。

`SaveInvitationImage.tsx` 在点击保存后才生成图片；支持文件分享的移动浏览器在下一次点击调用系统菜单，保留所需的用户激活。其他环境提供图片预览、长按提示或下载。取消分享不声明已保存，其他失败仍可下载；对象 URL 在组件卸载时回收。完整操作与验证命令见 [README.md](README.md#阅读与保存)。

## 素材与维护边界

公开构建从通用 SVG 生成占位图，站内使用本地 WebP。本地照片按仓库清单放入根目录 `.local/photos/yusheng/`；以下文件名保留原有选片映射。

| 站内名     | 用户原片     |
| ---------- | ------------ |
| courtyard  | 5A8A5952.jpg |
| promise    | 5A8A1419.jpg |
| together   | 5A8A6634.jpg |
| a-long-way | 5A8A6627.jpg |

每张照片有 640 与 1280 目标宽度的版本，`a-long-way` 另有 2048 版本。宽度不超过 700px 的竖屏封面使用 `together`，其他视口使用 `a-long-way`。手机竖屏保留人物取景，完整照片绘在同一连续封面上。更换照片时同步核对 `public/images/`、`WeddingPhoto.vue`、`CoverFront.vue`、照片替代文本与 `src/styles/cover.css` 的取景。

婚礼事实以 `src/data/wedding.ts` 为入口；时间按所在地 UTC+8 解释。地图始终搜索完整店名，厅号另列；目前没有已核实坐标。事实变更还需核对封面、正式邀请与页脚中的固定日期和姓名、页面标题、图片预览替代文本和实际 PNG。

中文子集由 `scripts/subset-font.mjs` 维护，独立数字子集保留来源记录与现有文件。普通构建只使用已保存的 WOFF2；再生成命令、授权和文件结构见 [README 字体维护](README.md#字体维护)。字体更新后检查“喆”“囍”、短屏换行与导出图片。

构建与部署见 [README.md](README.md#检查与部署)。源码与字体保留在仓库，照片、构建产物和验证证据由本地流程生成。更换素材后应检查手机取景、地图搜索和 PNG；构建通过不代表已完成目标手机上的系统分享或导航检查。
