# 婚礼背景音乐

将婚礼音乐保存为 `wedding-song.mp3` 并放在本目录，首屏音乐卡会自动启用播放按钮。

歌曲名称和演唱者在 `src/data/wedding.ts` 的 `music` 对象中修改：

- `title`：歌曲名称
- `artist`：演唱者
- `src`：音频路径，默认无需修改

建议使用兼容性较好的 MP3，控制文件大小，网页不会自动播放，需宾客点击音乐卡片中的动态条形按钮开始播放。